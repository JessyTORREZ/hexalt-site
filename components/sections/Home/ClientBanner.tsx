import React from 'react';

const ClientBanner: React.FC = () => {
  return (
    <div style={{ height: '100px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      {/* Future client logos will be displayed here */}
    </div>
  );
};

export default ClientBanner;